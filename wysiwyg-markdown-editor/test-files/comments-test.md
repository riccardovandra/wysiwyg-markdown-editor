# Comments test

This paragraph has {==a highlighted phrase==}{>>Make this less vague<<} in the middle, and the rest of the sentence continues after it.

Another paragraph with a thread: {==the quarterly number==}{>>Double-check this figure<<}{>>Claude: Verified against the Q2 report, it is 12%.<<} and more text.

A paragraph that carries an anchor-only comment.{>>Rewrite this whole paragraph in the active voice<<}

```ts
const total = items.reduce((sum, item) => sum + item.price, 0);
```

Final paragraph without comments so there is something to select for a new one.
